{
  "buildCommand": "cd .. && pnpm install && pnpm --filter shared build && pnpm --filter client build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
