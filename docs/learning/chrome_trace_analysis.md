# Chrome DevTools Performance 트레이스 분석 가이드

## 개요
IO 게임처럼 60fps가 필수인 게임에서 초기 렉(프레임 드랍)은 사용자 이탈의 주요 원인입니다. Chrome DevTools의 Performance 탭을 활용하면 정확한 병목 지점을 찾을 수 있습니다.

## 트레이스 파일 생성하기

### 1. 녹화 시작
1. Chrome에서 게임 페이지 열기
2. F12 → Performance 탭
3. ⚙️ 설정에서 "Screenshots" 체크
4. 🔴 녹화 버튼 클릭 후 페이지 새로고침

### 2. 분석할 구간 선택
- 초기 로딩: 페이지 로드 ~ 첫 프레임 렌더링
- 게임 시작: 로비 → 게임 화면 전환 시점

### 3. JSON 내보내기
- 녹화 완료 후 우클릭 → "Save profile..."
- 대용량(수십 MB)일 수 있으므로 분석 스크립트 활용 권장

## 핵심 지표 이해하기

### Long Task (50ms 이상)
```
❌ 나쁜 예: RunTask 225ms
✅ 좋은 예: RunTask 40ms
```
- 메인 스레드를 50ms 이상 블로킹하면 **Long Task**로 분류
- 60fps 기준 1프레임 = 16.67ms, 50ms = 약 3프레임 드랍

### 주요 이벤트 유형
| 이벤트 | 의미 | 원인 |
|-------|------|------|
| EvaluateScript | JS 파싱/실행 | 번들 크기, 동기 실행 |
| FunctionCall | 함수 호출 | 무거운 초기화 로직 |
| FireAnimationFrame | RAF 콜백 | 렌더링 부하 |
| Layout/Paint | 레이아웃 계산 | DOM 조작, 리플로우 |

## 실전 분석 예시

### ChaosRPS.io 분석 결과
```
1. ParseHTML + EvaluateScript: 225ms → 초기 번들 실행
2. FunctionCall: 205ms → Phaser 게임 초기화
3. FireAnimationFrame: 81ms → 첫 프레임 렌더링
```

### 원인 파악
- **225ms**: 메인 번들에 Phaser(1.4MB) 포함
- **205ms**: Object Pool 25개 동기 생성
- **81ms**: 모든 플레이어 + 그리드 한번에 렌더링

## 자동화 분석 스크립트

트레이스 파일이 너무 크면(60MB+) 스크립트로 분석:

```javascript
// Long Task 추출 예시
const events = trace.traceEvents;
const longTasks = events.filter(e => 
  e.dur && e.dur / 1000 >= 50  // 50ms 이상
);
longTasks.sort((a, b) => b.dur - a.dur);
console.log('Top Long Tasks:', longTasks.slice(0, 10));
```

## 결론
트레이스 분석은 **감이 아닌 데이터**로 최적화 우선순위를 정하게 해줍니다. "느린 것 같다"가 아닌 "205ms Long Task가 있다"로 문제를 정의할 수 있습니다.

## 참고 자료
- [Chrome DevTools Performance Analysis](https://developer.chrome.com/docs/devtools/performance/)
- [Long Tasks API](https://developer.mozilla.org/en-US/docs/Web/API/PerformanceLongTaskTiming)
